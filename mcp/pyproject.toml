[project]
name = "mcp-eviction-server"
version = "0.1.0"
description = "FastMCP server for Franklin County eviction database"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=0.1.0",
    "psycopg[binary]>=3.1.0",
    "httpx>=0.27.0",
    "fastmcp"
]
